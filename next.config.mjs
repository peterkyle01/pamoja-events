/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "zldueflrxvxbusygwauh.supabase.co",
      },
    ],
  },
};

export default nextConfig;
